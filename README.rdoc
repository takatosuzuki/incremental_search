== インクリメンタルサーチ

# 概要
検索した時、検索窓の下にリアルタイムで検索結果が表示される。
- - -

## やったこと
- - -
- url:    
-- 読み込むページurl
- type:   
-- データ取得のためGET
- data:   
-- サーバーへ送信するデータ
- keyup:  
-- キーの操作が起きたとき
- $.trim: 
-- 文字列の先頭と末尾から、空白を除去します。
- val():  
-- value属性を返す
- if(preInput !== input){:
-- 検索窓の文字列が変更されたらという条件式にする
- preFunc = setTimeout(ajaxSearch, 500);
-- 入力時に毎回処理が発生しないようにする
--- preFunc = null, 初期化
